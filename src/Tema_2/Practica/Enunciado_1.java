package Tema_2.Practica;
/** Dado un número entero aleatorio entre 1 y 100 que será almacenado en la variable randomNumber,
 * mostrar por pantalla si este número es primo o no. */

public class Enunciado_1 {
    public static void main(String[] args) {
        /** Declaramos la variable randomNumber
         *  que se inicialice con un número ramdom del 1 al 100 */
        Integer randomNumber = (int) (Math.random()*100)+1; // Uso del método random
        Boolean esPrimo = true; // Inicializamos variable esPrimo a true

        /** Creamos un bucle for  */

    }
}
